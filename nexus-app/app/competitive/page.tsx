import CompetitivePanel from '@/components/CompetitivePanel';

export const metadata = { title: 'Competitive Intel — NEXUS AI' };

export default function CompetitivePage() {
  return <CompetitivePanel />;
}
